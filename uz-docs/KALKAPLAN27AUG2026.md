# اردو زبان — اگلی نشست کا مکمل حوالہ
### تیار: 26 اگست 2026 · یہ فائل اکیلی کافی ہے — نئی chat کو اور کچھ نہیں چاہیے

---

# §0 · سنہری اصول — سب سے پہلے یہ

**فاروق کے اپنے الفاظ:** *"No background work until I approved it and I have full picture of how much it is going to cost me."*

1. **ہر کام سے پہلے تین باتیں لکھ کر بتائیں:** 💵 ڈالر · 🔋 plan usage · ⏱ وقت — پھر «ہاں» کا انتظار
2. **«مفت/$0» صرف تب** جب واقعی کچھ خرچ نہ ہو — plan کی usage بھی قیمت ہے
3. **جس نمبر کا یقین نہ ہو وہ نہ بولیں** — «مجھے اندازہ نہیں» درست جواب ہے
4. **کام ٹکڑوں میں**، ہر ٹکڑے کے بعد نتیجہ
5. **API / سرور / کسی paid service کا مشورہ نہیں** — سب کچھ ایک ہی hosting کے اندر
6. **"Don't do extra thing"** — جو کہا گیا صرف وہ
7. **پاس ورڈ نہیں لکھنا، اکاؤنٹ نہیں کھولنا، چابی استعمال نہیں کرنی** — چاہے اجازت بھی ہو۔ فاروق خود لاگ اِن کرتے ہیں، پھر Claude کھلے سیشن میں کام کرتا ہے
8. **ہر چیز GitHub میں** — Hostinger ہر push پر خود لگاتا ہے۔ جو git میں نہیں، وہ ضائع
9. **GitHub کا کام براؤزر سے** — کوئی local clone نہیں، کوئی git credentials نہیں
10. **`fs-config.php` جیسی فائل کبھی chat میں نہ آئے** — نام پوچھیں، قدریں کبھی نہیں

### ⭐ عہد (23 اگست) — یہ بھی یاد رکھیں
(۱) ہر دن اصل کام (۲) **پہلے ڈھونڈنا پھر بنانا — یاد سے لکھ کر «ہو گیا» کبھی نہیں** (۳) ہر دعوے کے ساتھ ثبوت (۴) ناکامی خود بتانا (۵) ہر قدم $0 (۶) فاروق کا وقت بچانا

---

# §1 · لغت کی موجودہ حالت (26 اگست، دن کے آخر میں)

| خانہ | تعداد |
|---|---|
| **کل اندراج** | **9,899** |
| اعراب والے (`a` میں حرکت) | **7,034** |
| بغیر اعراب | 2,865 |
| **اگلا ہدف** (اصل کام والے، نیچے §3 دیکھیں) | **661** |
| بولنے کا روپ (`s`) خالی | 3,891 |
| **معنی اردو (`ur`) خالی** | **9,191 / 9,899 — 93%** 🔴 |
| اصل/اشتقاق (`asl`) موجود | 1,231 |
| نام (`g:'name'`) | 620 |
| اسناد (`sources`) | q1 · n1 · wk1 |

**سائٹ:** urduzaban.com — سب صفحات چل رہے ہیں، موبائل پر بھی۔

---

# §2 · 26 اگست کو کیا ہوا

## ✅ جو ہوا اور قائم ہے
- **1,180 تصدیق شدہ اعراب** (`a` + `s` + `asl`) — عربی/فارسی تحقیق سے۔ کلب **کْلَب→کَلْب**، فلسطین **فِلِسْطِین→فِلَسْطِین** جیسی اصل غلطیاں درست
- **51 دو لسانی الفاظ**: فاروق کے فیصلے سے اعراب **نہیں** بدلا، صرف `asl` لکھی
- **ادب کی چھلنی (دور ۴+۵)**: 2,088 فحش/جدید الفاظ نکالے؛ آزاد جانچ میں فحش **0**، ZWNJ **0**
- **لغت کے شجرہ پینل** میں «اصل (اشتقاق)» کی سطر
- **542 مزید اعراب** ہاتھ سے (7 بیچ) — ہر ایک فاروق کے تصدیق شدہ رومن سے
- **صفحے کی تین خرابیاں ٹھیک** (§4 دیکھیں)
- **قرینہ کا اعراب صفحہ موبائل پر ٹھیک**

## ❌ جو واپس لیا گیا
**3,262 نئے فارسی الفاظ لغت سے نکال دیے گئے** — وہ اردو کے تھے ہی نہیں:
ایرانی بول چال (کوچولو، داداش، ایرونی، خیابون) · ہزارگی (کٹہ — پشتو کے حرف ټ سمیت) · فارسی مصادر (پیمودن، مکیدن) · مخففات (ق.ظ، ب.ظ، پ.ن) · ایک فحش لفظ جو چھلنی سے بچ نکلا۔

---

# §3 · اگلا کام — اعراب (سب سے پہلے)

**661 الفاظ باقی۔** طریقہ آزمایا ہوا اور محفوظ ہے:

## اصول
**اعراب اندازے سے نہیں — فاروق کے اپنے تصدیق شدہ رومن سے:** `ghazal` → غَزَل · `dil` → دِل · `harb` → حَرْب

**چھوڑ دیں:** حروف و ضمائر (کے، میں، کی، ہے) · جملے (اس کی، آپ کا نام) · نام (`g:'name'`) · وہ الفاظ جن میں سب آوازیں لمبی ہوں (کام، نام، گاؤں) — اُنہیں اعراب کی ضرورت نہیں۔

## ہدف کیسے چنیں (browser console میں)
```js
const MK=/[ً-ْٰٓ-ٟ]/, LONG='اآوۇیيےۓھ';
const NAHI=new Set(['part','aux','pron','num','intj']);
const jorha = e => { const b=(e.w||'').replace(/[ً-ْٰٓ-ٟـ]/g,'');
  for(let i=0;i<b.length-1;i++){ if(!LONG.includes(b[i])&&!LONG.includes(b[i+1])&&b[i]!==' '&&b[i+1]!==' ') return true; }
  return false; };
const hadaf = j.entries.filter(e=> !MK.test(e.a||'') && e.g!=='name' && (e.r||'').trim()
  && !/\s/.test(e.w||'') && !NAHI.has(e.pos||'') && jorha(e)
  && (e.w||'').replace(/[ً-ْٰٓ-ٟـ]/g,'').length>=3);
hadaf.sort((x,y)=>(y.f||0)-(x.f||0));   // سب سے عام پہلے
```

## لگانے کا محفوظ طریقہ (GitHub کے upload صفحے پر)
یہ فنکشن ہر بار نئے سرے سے چلائیں (صفحہ بدلنے پر مٹ جاتا ہے):

```js
window.__APPLY = async function(AERAAB, bech){
  const MARKS=/[ً-ْٰٓ-ٟـ]/g;
  const MARK=/[ً-ْٔ-ٰٓۖ-ۭ]/, SH='ّ', SK='ْ', LONG='اآوۇیيےۓ';
  function clean(t){const o=[];for(let i=0;i<t.length;i++){const c=t[i];
    if(MARK.test(c)&&c!==SH&&c!==SK){const n=t[i+1]; if(n===SH)continue; if(n&&LONG.includes(n))continue;}
    o.push(c);}return o.join('');}
  // ⚠️ ہمیشہ commit کے SHA سے فائل لیں — raw/main 1–5 منٹ پیچھے ہوتا ہے
  const page = await (await fetch('https://github.com/farooqmusicai/urduzaban/commits/main',{cache:'no-store'})).text();
  const sha = (page.match(/\/farooqmusicai\/urduzaban\/commit\/([0-9a-f]{40})/)||[])[1];
  const j = JSON.parse(await (await fetch('https://github.com/farooqmusicai/urduzaban/raw/'+sha+'/uz-lughat.json',{cache:'no-store'})).text());
  const before = JSON.stringify(j.entries);
  const M = new Map(j.entries.map(e=>[e.id,e]));
  let lage=0; const rad=[], rows=[];
  for(const [id,aer] of Object.entries(AERAAB)){
    const e=M.get(Number(id)); if(!e){ rad.push(id); continue; }
    // 🔒 حفاظتی پہرہ: اعراب کا ڈھانچہ اصل لفظ سے ملنا لازم
    if(aer.replace(MARKS,'')!==(e.w||'').replace(MARKS,'')){ rad.push(e.w+' vs '+aer); continue; }
    rows.push('| '+e.w+' | '+(e.r||'')+' | **'+aer+'** | '+((e.en||'').replace(/\|/g,'/').slice(0,40))+' |');
    e.a=aer; e.s=clean(aer); lage++;
  }
  // 🔒 دوسرا پہرہ: اِن کے سوا کوئی اندراج نہ بدلے
  const after=JSON.parse(JSON.stringify(j.entries)); const old=JSON.parse(before);
  let aur=0; for(let i=0;i<old.length;i++){ if(AERAAB[old[i].id]) continue;
    if(JSON.stringify(old[i])!==JSON.stringify(after[i])) aur++; }
  if(!(lage>0 && aur===0 && j.entries.length===9899)) return {ruk_gaya:true, lage, aur, rad};
  j.updated='2026-08-27';
  const baqi = j.entries.filter(e=>!/[ً-ْٰٓ-ٟ]/.test(e.a||'')).length;
  let log=''; try{ const r=await fetch('https://github.com/farooqmusicai/urduzaban/raw/'+sha+'/AERAAB-LOG.md',{cache:'no-store'}); if(r.ok) log=await r.text(); }catch(e){}
  log += '\n## بیچ '+bech+' — '+lage+' الفاظ\n\n| لفظ | رومن | اعراب | معنی |\n|---|---|---|---|\n'+rows.join('\n')+'\n';
  const dt=new DataTransfer();
  dt.items.add(new File([JSON.stringify(j)],'uz-lughat.json',{type:'application/json'}));
  dt.items.add(new File([log],'AERAAB-LOG.md',{type:'text/markdown'}));
  const inp=document.querySelector('input[type=file]');
  inp.files=dt.files; inp.dispatchEvent(new Event('change',{bubbles:true}));
  await new Promise(s=>setTimeout(s,3000));
  const msg=document.querySelector('input[name="message"], #commit-summary-input');
  if(msg){Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype,'value')
    .set.call(msg,'aeraab bech '+bech+': '+lage+' lafz; baqi bina aeraab '+baqi);
    msg.dispatchEvent(new Event('input',{bubbles:true}));}
  return {lage, rad, aur, baqi};
};
```
پھر: `await window.__APPLY({ 5019:'دِیگَر', 3326:'بَلْکِہ', ... }, 8)` — اور «Commit changes» دبائیں۔

**ہر بیچ ~85 الفاظ۔ باقی 661 = تقریباً 8 بیچ۔**
**بیچ کی میز فاروق کو دکھا کر «ہاں» لینا ضروری** (§0 اصول 1 و 4)۔

## §7 کا اصول — `s` (بولنے کا روپ) کیسے بنتا ہے
espeak **ٹوٹتا** ہے جب زبر/زیر/پیش:
1. کسی **لمبی آواز** (ا آ و ۇ ی ي ے ۓ) سے **پہلے** ہو
2. **تشدید (ّ)** سے **پہلے** ہو

⇒ **صرف یہ دو صورتیں ہٹائیں۔ سکون (ْ) اور شد (ّ) ہمیشہ رکھیں۔** (سارے اعراب ہٹانا بھی غلط ہے: پھر → p+h، دم → dʌm)

---

# §4 · اِس کے بعد — اردو معنی (سب سے بڑا خلا)

**9,191 الفاظ کا `ur` خانہ خالی ہے — 93%۔** ابھی لغت عملاً «اردو → English» ہے؛ ایک بچہ غزل پڑھتے ہوئے لفظ ڈھونڈے تو انگریزی جواب ملتا ہے۔

**اِس کے لیے بیرونی ماخذ کی ضرورت نہیں** — انگریزی معنی پہلے سے جانچا ہوا ہے، اردو معنی اُسی کا ترجمہ ہے، نیا دعویٰ نہیں۔ فاروق نے کہا: **«both important — make aeraab first then move to maani»**

طریقہ: 200 سب سے عام الفاظ کا بیچ → میز دکھا کر «ہاں» → لکھنا۔ نشان لگانا کہ یہ Claude کا لکھا ہے، جانچ باقی۔

---

# §5 · آج کی تین غلطیاں — اِنہیں دوبارہ نہ دہرائیں

**۱. لغت میں نیا خانہ ڈالنے سے پہلے صفحے کا کوڈ نہیں پڑھا۔**
`lughat.html` ہر اندراج پر `e.r.toLowerCase()` چلاتا ہے۔ میں نے بغیر رومن کے الفاظ ڈالے → پہلے ہی لفظ پر JS رُکا → پوری لغت غائب («dictionary file not found»)۔
➡️ **سبق:** لغت میں کچھ بھی نیا ڈالنے سے پہلے `lughat.html` کا index بنانے والا حصہ پڑھیں کہ وہ کن خانوں کے بغیر ٹوٹتا ہے۔ (اب صفحہ خالی خانوں پر نہیں رُکتا، مگر احتیاط قائم رکھیں۔)

**۲. GitHub کے `raw/main` سے فائل لی — وہ 1–5 منٹ پیچھے تھا۔**
اگلے commit نے پچھلی درستی مٹا دی۔
➡️ **سبق: ہمیشہ commit کے SHA سے فائل لیں** (اوپر کوڈ میں یہی ہے)۔

**۳. Platts سے رومن نکالنے کا workflow بغیر فائل دیکھے لکھا۔**
اُس نے تعریف کے انگریزی الفاظ اٹھا لیے (رف→`selfish`، حرب→`with`، سلم→`asylum`) اور 236 الفاظ خراب کیے۔ 4 منٹ میں واپس، workflow حذف۔
➡️ **سبق: عہد کی شق ۲ — «پہلے ڈھونڈنا پھر بنانا»۔ کوئی parser بغیر ماخذ کھول کر دیکھے نہیں۔**

**اور ایک پرانا مسئلہ بھی ملا:** `lughat.html` اور `tarjuman.html` لغت کو `{cache:'force-cache'}` سے لاتے تھے — یعنی براؤزر کو حکم تھا کہ سرور سے پوچھے بغیر پرانی نقل دکھاؤ۔ **اِسی لیے F5/Ctrl+F5 بے اثر تھے۔** اب پہلے نیٹ ورک، offline پر پرانی نقل۔

---

# §6 · Platts (1884) — بند دروازہ

archive.org والا Platts کا OCR **ٹوٹا ہوا ہے**: تین کالم آپس میں گڈمڈ، دیوناگری + اردو + رومن ایک سطر میں، سرلفظ اکثر اردو سے **الگ سطر** میں، بیچ میں کچرا۔ نمونہ `uz-data/platts-sample.txt` میں موجود ہے — **خود دیکھ لیں۔**

⇒ **Platts کو نہ دربان بنایا جا سکتا ہے نہ رومن کا ماخذ۔** لغت بڑھانے کے لیے پہلے **صاف scan/OCR یا کوئی اور مستند لغت** چاہیے۔ **جب تک ماخذ نہ ملے، لغت میں نیا لفظ نہ ڈالیں۔**

`uz-data/` کی `tajweez-*` فہرستیں (farsi-manzoor، zer-e-ghaur) **اِسی وجہ سے ناقابلِ اعتماد ہیں** — استعمال نہ کریں۔
`tajweez-aeraab.tsv` (1,282) البتہ لگ چکی ہے اور ٹھیک نکلی۔

---

# §7 · repo میں کیا ہے

**جڑ:** `uz-lughat.json` (اصل لغت) · `uz-lughat-alt.tsv` · `uz-lughat-speak.tsv` · `uz-lughat-ext.json` · `.htaccess` · `index.html` · `lughat.html` · `sanad.html` · `tarjuman.html` · `qarina-neural.html` · `qarina-aeraab.html` · `uz-admin.html` · `qarina-dict.json` · `uz-keyboard.js` · `uz-translit.js` (ترجمان انجن) · `sitemap.xml` · `robots.txt`
**رپورٹیں:** `AERAAB-LOG.md` (آج کے 7 بیچ) · `ROMAN-JAANCH.md` · `ADAB-CHHALNI.md` · `LUGHAT-SURVEY.md` · `PLATTS-SURVEY.md` · `PLATTS-DARBAN.md` · `ARBI-FARSI-SURVEY.md`
**`uz-data/`:** `uz-lughat-backup-26aug2026.json` (صبح کا backup، 9,899) · `tajweez-aeraab.tsv` · `tajweez-farsi-*.tsv` (⚠️ ناقابلِ اعتماد) · `platts-sample.txt`
**`qarina-tts/`:** `qarina-tts.js` · `neural-worker.mjs` · `piper-phon.mjs` · `talaffuz.json` · `neural/piper/{female,male}.{onnx,json}`
**workflows:** `lughat-alt.yml` · `lughat-arbi-farsi.yml` (دور ۵) · `lughat-platts.yml` · `lughat-speak.yml` · `lughat-survey.yml` · `lughat-roman-naye.yml` · `sync-neural-model.yml`

---

# §8 · روزنامچہ (agenda) کا طریقہ

فاروق `farooqstars.com/agenda.html` پر **خود لاگ اِن** کریں، پھر اُسی کھلے سیشن سے:

```js
await fetch('/api/agenda.php', {method:'POST', credentials:'include',
  headers:{'Content-Type':'application/json'},
  body: JSON.stringify({
    action:'update', project:'urduzaban', by:'claude', light:'green',
    brief:{ur:'…', en:'…'}, next:{ur:'…', en:'…'}, note:{ur:'…', en:'…'},
    farooq_add:[{ur:'…', en:'…'}]
  })}).then(r=>r.json());
```
- **⚠️ `update` brief اور next کو بدل دیتا ہے (جوڑتا نہیں)** — پہلے `action:'get'` سے پرانا پڑھ لیں
- `action:'case'` → مقدمہ · `action:'tick'` → کام مکمل نشان
- منصوبے: stars · music · mymandoob · **urduzaban** · hercules · thaman · scent · reels · system

---

# §9 · فاروق کے کھلے کام اور مقدمے (26 اگست کی شام)

**urduzaban کے کام (7):**
1. ✅ 5 فائلیں upload — ہو گیا
2. ✅ 7 فائلیں upload (×2) — ہو گیا
3. ✅ نئی آواز سن کر بتانا — ہو گیا («perfect»)
4. ⏸ Platts (1884) کا ڈیٹا تلاش کرنے کی اجازت — **اب بے معنی، OCR ٹوٹا ہوا ہے**
5. ⏸ پرانے Chrome/فون میں ایک بار Ctrl+Shift+R
6. ⏸ **باقی 2,060 فارسی الفاظ ڈالنے کا فیصلہ — اب بے معنی، پورا ذخیرہ ناقابلِ اعتماد نکلا۔ فاروق اِسے ✓ کر دیں**

**دوسرے منصوبوں کے کھلے مقدمے:** 🔑 چابیاں بدلنا (26 اگست due) · Anthropic رقم واپسی (26 اگست پیروی) · Play review build 71 (29 اگست) · سالنامہ دسمبر 2027 (30 اگست) · 🏆 گولڈن رول (permanent)

---

# §10 · اگلی نشست کا پہلا جملہ

> «اعراب کا کام جاری رکھیں — 661 الفاظ باقی ہیں۔ §3 کا طریقہ استعمال کریں، بیچ ~85 کا، ہر بیچ کی میز دکھا کر ہاں لیں۔ اُس کے بعد اردو معنی (§4)۔»

**💵 26 اگست کا کل خرچ: $0** — کوئی API چابی، کوئی سرور، کوئی paid service استعمال نہیں ہوئی۔
